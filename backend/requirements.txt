# TP全屋家居官网 后端依赖（对齐开发技术文档 §2.2 技术选型）
# 安装：pip install -r requirements.txt

# --- Web 框架 ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- 数据库 ORM 与迁移 ---
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1          # 生产 PostgreSQL 驱动（postgresql+psycopg）

# --- 数据校验 ---
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.0          # EmailStr 校验

# --- 认证与安全 ---
passlib[bcrypt]>=1.7.4
bcrypt>=4.0,<4.1              # 锁版本避免 passlib 兼容告警
python-jose[cryptography]>=3.3

# --- 上传与图片 ---
python-multipart>=0.0.9       # multipart 表单解析
pillow>=10.0                  # 图片压缩/缩略图/文件头校验

# --- 测试 ---
pytest>=8.0
httpx>=0.27
